---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-planner
  namespace: default
  labels:
    app: content-planner
spec:
  replicas: 2
  selector:
    matchLabels:
      app: content-planner
  template:
    metadata:
      labels:
        app: content-planner
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: content-planner
          image: rummeltech/content-planner:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8060
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: services-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8060
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8060
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: content-planner
  namespace: default
  labels:
    app: content-planner
spec:
  selector:
    app: content-planner
  ports:
    - name: http
      port: 80
      targetPort: 8060
  type: ClusterIP
