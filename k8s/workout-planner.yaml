---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workout-planner
  namespace: default
  labels:
    app: workout-planner
spec:
  replicas: 2
  selector:
    matchLabels:
      app: workout-planner
  template:
    metadata:
      labels:
        app: workout-planner
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: workout-planner
          image: rummeltech/workout-planner:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: services-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: workout-planner
  namespace: default
  labels:
    app: workout-planner
spec:
  selector:
    app: workout-planner
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
