---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-planner
  namespace: default
  labels:
    app: work-planner
spec:
  replicas: 2
  selector:
    matchLabels:
      app: work-planner
  template:
    metadata:
      labels:
        app: work-planner
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: work-planner
          image: rummeltech/work-planner:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8040
          envFrom:
            - configMapRef:
                name: services-config
            - secretRef:
                name: services-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8040
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8040
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: work-planner
  namespace: default
  labels:
    app: work-planner
spec:
  selector:
    app: work-planner
  ports:
    - name: http
      port: 80
      targetPort: 8040
  type: ClusterIP
